= Detect
Run onload (static?) checks

URL fragments probably has urllib injection someplace, so investigate more comptrhensive Request build.

Deprecate settings PLEASE
Put the View Loader into the generator
Make robustly generate, even with errors


? Full rename

